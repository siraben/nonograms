# Frontend (Vite)
VITE_TURNSTILE_SITE_KEY=
VITE_REQUIRE_CAPTCHA=0

# Wrangler local dev (Pages Functions)
# These are read by wrangler when running `wrangler pages dev` if you put them in `.dev.vars`.
TURNSTILE_SECRET_KEY=
REQUIRE_CAPTCHA=0
